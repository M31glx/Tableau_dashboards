* text=auto

# Tableau workbooks are XML
*.twb text eol=lf diff=xml

# Optional: hide from language stats
# *.twb linguist-vendored

# Optional: auto-format on commit (requires filter setup)
# *.twb filter=twbformat
